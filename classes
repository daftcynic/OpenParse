Receiver
Responder (HTTP style responder for different types html, json, xml, etc)
Request (Perhaps make a class for each HTTP/1.1 POST, GET, PUT, DELETE, HEAD, OPTIONS
RequestFactory
DBConnector (Interface)
    MongoDBConnector
    etc
Queries (Interface)



PlugInController -- to handle user created dbconnectors and queries


Should be like parse - but all objects can be made from scratch from the first post request
    // need to figure out security, extra api header


requests need to look in header for instead of putting them in the request params
    "X-Foo-Barr-Api-User-Id: ${ID}"
    "X-FOO-Barr-API-Key: ${id}"